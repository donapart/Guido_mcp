# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║                    MCP AGENT WORKBENCH - KONFIGURATION                       ║
# ║                                                                              ║
# ║  Diese Datei enthält ALLE Einstellungen für den MCP Agent.                  ║
# ║  Kopiere diese Datei nach ".env" und passe die Werte an.                    ║
# ║                                                                              ║
# ║  🔐 WICHTIG: Diese Datei enthält sensible Daten!                            ║
# ║     - Niemals in Git committen                                               ║
# ║     - Niemals teilen oder veröffentlichen                                    ║
# ╚══════════════════════════════════════════════════════════════════════════════╝


# ══════════════════════════════════════════════════════════════════════════════
# 🧠 KI-PROVIDER KONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════
# 
# Der Agent benötigt mindestens EINEN KI-Provider um zu funktionieren.
# Du kannst auch beide konfigurieren und mit LLM_MODEL wechseln.
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ OPENAI                                                                      │
# │ ─────────                                                                   │
# │ Website: https://platform.openai.com/api-keys                              │
# │ Modelle: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-3.5-turbo                   │
# │ Kosten:  ~$0.01-0.03 pro 1000 Tokens (gpt-4o)                              │
# │                                                                             │
# │ Empfohlen für: Beste Tool-Nutzung, schnelle Antworten                      │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Dein OpenAI API-Key (beginnt mit "sk-")
# Erstellen: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ ANTHROPIC (Claude)                                                          │
# │ ──────────────────                                                          │
# │ Website: https://console.anthropic.com/                                    │
# │ Modelle: claude-sonnet-4-20250514, claude-3-5-sonnet, claude-3-opus        │
# │ Kosten:  ~$0.003-0.015 pro 1000 Tokens                                     │
# │                                                                             │
# │ Empfohlen für: Längere Kontexte, Code-Analyse, kreative Aufgaben           │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Dein Anthropic API-Key (beginnt mit "sk-ant-")
# Erstellen: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ MODELL-AUSWAHL                                                              │
# │ ──────────────                                                              │
# │                                                                             │
# │ OpenAI Modelle:                                                             │
# │   • gpt-4o              - Bestes Modell, schnell, multimodal               │
# │   • gpt-4o-mini         - Günstig, gut für einfache Aufgaben               │
# │   • gpt-4-turbo         - Stark, aber teurer                               │
# │                                                                             │
# │ Anthropic Modelle:                                                          │
# │   • claude-sonnet-4-20250514 - Neuestes, bestes Claude                     │
# │   • claude-3-5-sonnet   - Ausgewogen, gut für Code                         │
# │   • claude-3-opus       - Stärkstes Claude (teuer)                         │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Welches Modell soll standardmäßig verwendet werden?
LLM_MODEL=gpt-4o


# ══════════════════════════════════════════════════════════════════════════════
# 🐳 DOCKER KONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ DOCKER REMOTE HOST                                                          │
# │ ──────────────────                                                          │
# │                                                                             │
# │ Wenn Docker auf einem anderen Rechner läuft (z.B. NAS, Server),            │
# │ kannst du dich per TCP verbinden.                                           │
# │                                                                             │
# │ Formate:                                                                    │
# │   • tcp://IP:PORT      - Unverschlüsselt (nur im lokalen Netz!)            │
# │   • tcp://IP:2376      - Mit TLS (sicherer)                                │
# │   • unix:///var/run/docker.sock - Lokaler Socket (Linux/Mac)               │
# │                                                                             │
# │ Docker API aktivieren (auf dem Remote-Host):                               │
# │   1. /etc/docker/daemon.json bearbeiten:                                   │
# │      {"hosts": ["unix:///var/run/docker.sock", "tcp://0.0.0.0:2375"]}     │
# │   2. Docker neu starten: sudo systemctl restart docker                     │
# │                                                                             │
# │ ⚠️  WARNUNG: tcp://IP:2375 ohne TLS nur im vertrauenswürdigen LAN!         │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Adresse des Docker-Hosts (leer = lokaler Docker)
DOCKER_REMOTE_HOST=tcp://192.168.0.27:2375

# Timeout für Docker-Operationen in Sekunden
# Standard: 30, erhöhen bei langsamen Verbindungen
DOCKER_TIMEOUT=30


# ══════════════════════════════════════════════════════════════════════════════
# 🤖 OLLAMA KONFIGURATION (Lokale LLMs)
# ══════════════════════════════════════════════════════════════════════════════
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ OLLAMA                                                                      │
# │ ──────                                                                      │
# │                                                                             │
# │ Ollama ermöglicht das Ausführen von LLMs lokal oder auf eigenem Server.    │
# │ Keine API-Kosten, volle Kontrolle über deine Daten.                        │
# │                                                                             │
# │ Installation: https://ollama.ai                                            │
# │                                                                             │
# │ Beliebte Modelle:                                                           │
# │   • llama3.2           - Meta's neuestes Modell (8B Parameter)             │
# │   • llama3.2:70b       - Größere Version (benötigt viel RAM)               │
# │   • mistral            - Sehr schnell, gut für Chat                        │
# │   • codellama          - Spezialisiert auf Code                            │
# │   • nomic-embed-text   - Für Embeddings/Semantic Search                    │
# │                                                                             │
# │ Modell laden: ollama pull llama3.2                                         │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Ollama Server URL
# Lokal: http://localhost:11434
# Remote: http://192.168.0.27:11434
OLLAMA_HOST=http://192.168.0.27:11434

# Standard-Modell für Ollama-Anfragen
OLLAMA_DEFAULT_MODEL=llama3.2

# Timeout für Ollama-Anfragen (LLM-Generierung kann lange dauern)
OLLAMA_TIMEOUT=120


# ══════════════════════════════════════════════════════════════════════════════
# 🐙 GITHUB KONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ GITHUB PERSONAL ACCESS TOKEN                                                │
# │ ────────────────────────────                                                │
# │                                                                             │
# │ Ermöglicht Zugriff auf GitHub Repositories, Issues, Pull Requests.         │
# │                                                                             │
# │ Token erstellen:                                                            │
# │   1. GitHub → Settings → Developer Settings → Personal Access Tokens       │
# │   2. "Generate new token (classic)" oder "Fine-grained token"              │
# │   3. Benötigte Scopes: repo, read:org (für private Repos)                  │
# │                                                                             │
# │ Token-Typen:                                                                │
# │   • Classic (ghp_xxx) - Einfacher, mehr Rechte möglich                     │
# │   • Fine-grained      - Präzise Kontrolle pro Repository                   │
# │                                                                             │
# │ Link: https://github.com/settings/tokens                                   │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# GitHub Personal Access Token (beginnt mit "ghp_" oder "github_pat_")
GITHUB_TOKEN=

# GitHub API Timeout in Sekunden
GITHUB_TIMEOUT=30


# ══════════════════════════════════════════════════════════════════════════════
# 🌐 IONOS KONFIGURATION (Hosting & DNS)
# ══════════════════════════════════════════════════════════════════════════════
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ IONOS DNS API                                                               │
# │ ─────────────                                                               │
# │                                                                             │
# │ Ermöglicht die Verwaltung von DNS-Einträgen über die IONOS API.            │
# │                                                                             │
# │ API-Key erstellen:                                                          │
# │   1. IONOS Control Panel öffnen                                            │
# │   2. "Domains & SSL" → Domain auswählen                                    │
# │   3. "DNS" → "API" → "API-Schlüssel erstellen"                            │
# │                                                                             │
# │ Format: publicPrefix.secretKey (durch Punkt getrennt)                      │
# │ Beispiel: abc123.xyz789                                                    │
# │                                                                             │
# │ Docs: https://developer.hosting.ionos.de/docs/dns                          │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# IONOS API-Key (Format: prefix.secret)
IONOS_API_KEY=


# ══════════════════════════════════════════════════════════════════════════════
# 🔐 SSH KONFIGURATION (Remote Server)
# ══════════════════════════════════════════════════════════════════════════════
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SSH HOSTS                                                                   │
# │ ─────────                                                                   │
# │                                                                             │
# │ Konfiguriere SSH-Verbindungen zu Remote-Servern.                           │
# │ Jeder Host braucht 2-3 Einträge:                                           │
# │                                                                             │
# │   SSH_HOST_<NAME>=user@hostname:port                                       │
# │   SSH_PASSWORD_<NAME>=passwort          (Option A: Passwort)               │
# │   SSH_KEY_<NAME>=C:\pfad\zur\key        (Option B: SSH-Key)                │
# │                                                                             │
# │ Beispiele:                                                                  │
# │   SSH_HOST_IONOS=root@server.ionos.de:22                                   │
# │   SSH_PASSWORD_IONOS=mein-passwort                                         │
# │                                                                             │
# │   SSH_HOST_RASPBERRY=pi@192.168.0.100:22                                   │
# │   SSH_KEY_RASPBERRY=C:\Users\ich\.ssh\id_rsa                               │
# │                                                                             │
# │ ⚠️  SSH-Keys sind sicherer als Passwörter!                                  │
# │     Erstellen: ssh-keygen -t ed25519 -C "mcp-agent"                        │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# --- IONOS Server ---
# SSH_HOST_IONOS=root@dein-server.ionos.de:22
# SSH_PASSWORD_IONOS=dein-passwort
# oder
# SSH_KEY_IONOS=C:\Users\DeinName\.ssh\id_rsa

# --- Raspberry Pi ---
# SSH_HOST_PI=pi@192.168.0.100:22
# SSH_KEY_PI=C:\Users\DeinName\.ssh\pi_key

# --- NAS ---
# SSH_HOST_NAS=admin@192.168.0.50:22
# SSH_PASSWORD_NAS=nas-passwort


# ══════════════════════════════════════════════════════════════════════════════
# 📧 EMAIL KONFIGURATION (SMTP & IMAP)
# ══════════════════════════════════════════════════════════════════════════════
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ EMAIL SENDEN (SMTP)                                                         │
# │ ──────────────────                                                          │
# │                                                                             │
# │ SMTP-Server zum Versenden von E-Mails.                                     │
# │                                                                             │
# │ Gmail (App-Passwort erforderlich!):                                        │
# │   Host: smtp.gmail.com                                                     │
# │   Port: 587 (TLS) oder 465 (SSL)                                          │
# │   → Google-Konto → Sicherheit → App-Passwörter                            │
# │                                                                             │
# │ Outlook/Hotmail:                                                            │
# │   Host: smtp-mail.outlook.com                                              │
# │   Port: 587                                                                │
# │                                                                             │
# │ IONOS:                                                                      │
# │   Host: smtp.ionos.de                                                      │
# │   Port: 587 (TLS) oder 465 (SSL)                                          │
# │                                                                             │
# │ Eigener Server:                                                             │
# │   Host: mail.deine-domain.de                                               │
# │   Port: 587 (Standard mit STARTTLS)                                        │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# SMTP Server-Adresse
SMTP_HOST=smtp.gmail.com

# SMTP Port
# 587 = STARTTLS (empfohlen)
# 465 = SSL/TLS
# 25  = Unverschlüsselt (nicht empfohlen!)
SMTP_PORT=587

# SMTP Benutzername (meist die E-Mail-Adresse)
SMTP_USER=

# SMTP Passwort (bei Gmail: App-Passwort!)
SMTP_PASSWORD=

# TLS aktivieren (true/false)
SMTP_USE_TLS=true

# Standard-Absendername (optional)
SMTP_FROM_NAME=MCP Agent

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ EMAIL EMPFANGEN (IMAP)                                                      │
# │ ──────────────────────                                                      │
# │                                                                             │
# │ IMAP-Server zum Lesen von E-Mails.                                         │
# │                                                                             │
# │ Gmail:                                                                      │
# │   Host: imap.gmail.com                                                     │
# │   Port: 993                                                                │
# │   → IMAP in Gmail-Einstellungen aktivieren!                                │
# │                                                                             │
# │ Outlook/Hotmail:                                                            │
# │   Host: outlook.office365.com                                              │
# │   Port: 993                                                                │
# │                                                                             │
# │ IONOS:                                                                      │
# │   Host: imap.ionos.de                                                      │
# │   Port: 993                                                                │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# IMAP Server-Adresse
IMAP_HOST=imap.gmail.com

# IMAP Port (993 = SSL, Standard)
IMAP_PORT=993

# IMAP Benutzername (meist die E-Mail-Adresse)
IMAP_USER=

# IMAP Passwort (bei Gmail: App-Passwort!)
IMAP_PASSWORD=

# SSL aktivieren (true/false)
IMAP_USE_SSL=true


# ══════════════════════════════════════════════════════════════════════════════
# 🗄️ DATENBANK KONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ DATENBANKEN                                                                 │
# │ ───────────                                                                 │
# │                                                                             │
# │ Der Database-Server unterstützt verschiedene Datenbank-Typen:              │
# │   • SQLite     - Dateibasiert, kein Server nötig                          │
# │   • PostgreSQL - Professionell, OpenSource                                 │
# │   • MySQL      - Weit verbreitet                                           │
# │   • MSSQL      - Microsoft SQL Server                                      │
# │                                                                             │
# │ Verbindungen werden per Tool-Parameter übergeben, aber du kannst           │
# │ Standard-Verbindungen hier vorkonfigurieren.                               │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Standard SQLite-Datenbank (Dateipfad)
# Beispiel: d:\Daten\meine-datenbank.db
SQLITE_DEFAULT_DB=

# PostgreSQL Verbindung
# Format: postgresql://user:password@host:port/database
POSTGRES_URL=

# MySQL Verbindung
# Format: mysql://user:password@host:port/database
MYSQL_URL=

# MSSQL Verbindung
# Format: mssql://user:password@host:port/database
MSSQL_URL=


# ══════════════════════════════════════════════════════════════════════════════
# 📁 DATEISYSTEM KONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ PFADE & VERZEICHNISSE                                                       │
# │ ──────────────────────                                                      │
# │                                                                             │
# │ Konfiguriere Standard-Pfade für verschiedene Server.                       │
# │                                                                             │
# │ PROJECTS_BASE_PATH:                                                        │
# │   Basis-Verzeichnis für den Project-Manager.                               │
# │   Hier werden alle Projekte gescannt.                                      │
# │                                                                             │
# │ GIT_PROJECTS_PATH:                                                          │
# │   Standard-Pfad für Git-Operationen.                                       │
# │                                                                             │
# │ ALLOWED_PATHS (Sicherheit):                                                │
# │   Komma-getrennte Liste von Pfaden, auf die der Agent zugreifen darf.     │
# │   Leer = Alle Pfade erlaubt (Vorsicht!)                                   │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Basis-Pfad für Projekt-Scan
PROJECTS_BASE_PATH=d:\

# Standard-Pfad für Git-Repositories
GIT_PROJECTS_PATH=d:\

# Flutter-Projekte Pfad
FLUTTER_PROJECTS_PATH=d:\

# Erlaubte Pfade für Dateizugriff (leer = alle erlaubt)
# Beispiel: d:\Projekte,d:\Dokumente,c:\Users\ich\Desktop
ALLOWED_PATHS=


# ══════════════════════════════════════════════════════════════════════════════
# 🔍 WEB-SUCHE KONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ DUCKDUCKGO EINSTELLUNGEN                                                    │
# │ ────────────────────────                                                    │
# │                                                                             │
# │ Der Web-Search-Server nutzt DuckDuckGo (keine API-Keys nötig!).           │
# │                                                                             │
# │ Regionen:                                                                   │
# │   • de-de = Deutschland                                                    │
# │   • at-de = Österreich                                                     │
# │   • ch-de = Schweiz (deutsch)                                              │
# │   • us-en = USA (englisch)                                                 │
# │   • wt-wt = Weltweit (keine Filterung)                                     │
# │                                                                             │
# │ SafeSearch:                                                                 │
# │   • off      = Keine Filterung                                             │
# │   • moderate = Standard                                                    │
# │   • strict   = Strenge Filterung                                           │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Standard-Region für Suchergebnisse
SEARCH_REGION=de-de

# SafeSearch Level
SEARCH_SAFESEARCH=moderate

# Maximale Ergebnisse pro Suche
SEARCH_MAX_RESULTS=10


# ══════════════════════════════════════════════════════════════════════════════
# 🌐 WEB-SCRAPING KONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SCRAPING EINSTELLUNGEN                                                      │
# │ ──────────────────────                                                      │
# │                                                                             │
# │ Konfiguration für den Web-Scraping-Server.                                 │
# │                                                                             │
# │ User-Agent:                                                                 │
# │   Identifiziert den "Browser" bei Anfragen.                                │
# │   Manche Seiten blockieren unbekannte User-Agents.                         │
# │                                                                             │
# │ Timeout:                                                                    │
# │   Wie lange auf eine Seite gewartet wird.                                  │
# │                                                                             │
# │ ⚠️  Respektiere robots.txt und Terms of Service!                           │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# User-Agent für Web-Anfragen
SCRAPING_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36

# Timeout für Seitenabruf in Sekunden
SCRAPING_TIMEOUT=30

# Maximale Seitengröße in Bytes (5MB Standard)
SCRAPING_MAX_SIZE=5242880


# ══════════════════════════════════════════════════════════════════════════════
# 🦋 FLUTTER KONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ FLUTTER SDK                                                                 │
# │ ───────────                                                                 │
# │                                                                             │
# │ Flutter muss installiert und im PATH sein.                                 │
# │ Installation: https://flutter.dev/docs/get-started/install                 │
# │                                                                             │
# │ Prüfen: flutter doctor                                                     │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Flutter SDK Pfad (optional, wenn nicht im PATH)
# FLUTTER_SDK_PATH=C:\flutter

# Standard-Build-Modus: debug, profile, release
FLUTTER_BUILD_MODE=release

# Android SDK Pfad (optional)
# ANDROID_SDK_PATH=C:\Users\ich\AppData\Local\Android\Sdk


# ══════════════════════════════════════════════════════════════════════════════
# ⚙️ SYSTEM & LOGGING
# ══════════════════════════════════════════════════════════════════════════════
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ LOGGING                                                                     │
# │ ───────                                                                     │
# │                                                                             │
# │ Log-Level bestimmt wie viele Informationen ausgegeben werden.              │
# │                                                                             │
# │ Level:                                                                      │
# │   • debug   - Alles, sehr ausführlich (für Entwicklung)                   │
# │   • info    - Normal, wichtige Ereignisse                                  │
# │   • warning - Nur Warnungen und Fehler                                     │
# │   • error   - Nur Fehler                                                   │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Log-Level: debug, info, warning, error
LOG_LEVEL=info

# Debug-Modus aktivieren (mehr Ausgabe)
DEBUG=false

# Logs in Datei schreiben (optional)
# LOG_FILE=d:\Guido_mcp\logs\agent.log


# ══════════════════════════════════════════════════════════════════════════════
# 🔌 MCP SERVER KONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SERVER-VERWALTUNG                                                           │
# │ ─────────────────                                                           │
# │                                                                             │
# │ Diese Einstellungen beeinflussen wie MCP-Server gestartet werden.          │
# │                                                                             │
# │ Die Server-Definitionen selbst sind in mcp-servers.json.                   │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Pfad zur Server-Konfiguration
MCP_CONFIG_PATH=./mcp-servers.json

# Timeout für Server-Verbindungen (ms)
MCP_TIMEOUT=30000

# Max. Wiederholungen bei Fehlern
MCP_MAX_RETRIES=3

# Server-Start-Timeout (ms)
MCP_STARTUP_TIMEOUT=10000
