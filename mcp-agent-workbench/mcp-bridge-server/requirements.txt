# MCP Bridge Server - Requirements
# Hinweis: Die Bridge nutzt FastMCP aus dem `mcp` Paket.
mcp>=1.25.0

# Optional (aber empfohlen) für get_system_status():
psutil>=5.9.0

# Optional (empfohlen): lädt `agent/.env` automatisch, falls vorhanden
python-dotenv>=1.0.0
